---
title: "Custom Shape Layout"
sidebar_position: 3
description: "Custom Shape Layout starter kit with drag-to-draw CustomShapeEditor."
---

import WidgetDemo from '@site/src/components/WidgetDemo';
import { CustomShapeLayout } from '@app/App_CustomShapeLayout';
import { DEMO_MARKET_ID, DEMO_USERNAME } from '@site/src/constants';

# Custom Shape Layout

<WidgetDemo title="Live Demo" height="800px">
  <CustomShapeLayout marketId={DEMO_MARKET_ID} username={DEMO_USERNAME} />
</WidgetDemo>

**File:** `app/src/App_CustomShapeLayout.tsx`

The drag-to-draw belief editor for maximum expressiveness.

**Components:** `MarketStats` + `PasswordlessAuthWidget` -> `CustomShapeEditor` (full width, zoomable) -> `PositionTable`

**What it enables:** Users sculpt arbitrary probability distributions by dragging control points directly on the chart. Lock specific points, adjust resolution (5-25 control points), and see the belief curve update in real-time. The editor embeds its own consensus chart -- no separate chart component needed.

**Target audience:** Advanced traders, quantitative analysts. Highest friction, highest expressiveness.
